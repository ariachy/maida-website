{
  "name": "maida-admin-dependencies",
  "description": "Additional dependencies needed for the admin panel. Merge these into your existing package.json.",
  "dependencies": {
    "@prisma/client": "^5.10.0",
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "prisma": "^5.10.0",
    "@types/bcryptjs": "^2.4.6"
  },
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:studio": "prisma studio",
    "setup-admin": "ts-node --compiler-options '{\"module\":\"CommonJS\"}' scripts/setup-admin.ts"
  }
}
