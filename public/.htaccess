# ===========================================
# Maída Website - Optimized .htaccess
# Performance + Security + SEO
# ===========================================

# Enable rewrite engine
RewriteEngine On

# ===========================================
# HTTPS & WWW REDIRECTS
# ===========================================

# Redirect HTTP to HTTPS
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# Redirect www to non-www (optional - uncomment if needed)
# RewriteCond %{HTTP_HOST} ^www\.(.*)$ [NC]
# RewriteRule ^(.*)$ https://%1/$1 [R=301,L]

# ===========================================
# CLEAN URLs FOR NEXT.JS STATIC EXPORT
# ===========================================

# Remove trailing slash (except for directories)
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)/$ /$1 [L,R=301]

# Handle clean URLs for Next.js static export
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME}.html -f
RewriteRule ^(.*)$ $1.html [L]

# Custom 404
ErrorDocument 404 /404.html

# ===========================================
# SECURITY HEADERS
# ===========================================

<IfModule mod_headers.c>
    # -------------------------------------------
    # HSTS (HTTP Strict Transport Security) - NEW
    # -------------------------------------------
    # Forces browsers to use HTTPS for 1 year
    # Protects against: SSL stripping, downgrade attacks, cookie hijacking
    # 
    # How it works:
    # - Browser remembers to always use HTTPS for maida.pt
    # - Even if user types http://, browser auto-upgrades to https://
    # - includeSubDomains: applies to all subdomains too
    # - preload: allows submission to browser preload lists (optional)
    #
    # Rollout tip: Start with 1 week (604800), then increase to 1 year
    Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"

    # -------------------------------------------
    # COOP (Cross-Origin-Opener-Policy) - NEW
    # -------------------------------------------
    # Isolates your page from cross-origin documents
    # Protects against: Spectre attacks, cross-origin data leaks
    #
    # How it works:
    # - Prevents other sites from getting a reference to your window
    # - "same-origin-allow-popups" allows YOUR site to open popups
    #   (needed for Umai reservation widget, Stripe payments)
    Header always set Cross-Origin-Opener-Policy "same-origin-allow-popups"

    # -------------------------------------------
    # CSP (Content Security Policy) - FIXED FOR UMAI
    # -------------------------------------------
    # Controls which resources the browser can load
    # Protects against: XSS (Cross-Site Scripting) attacks
    #
    # How it works:
    # - Whitelist of allowed sources for scripts, styles, images, etc.
    # - Browser blocks anything not on the whitelist
    #
    # Note: 'unsafe-inline' and 'unsafe-eval' are needed for:
    # - Google Tag Manager (dynamic script injection)
    # - Google Maps (eval for map rendering)
    # - Next.js/Framer Motion (inline styles)
    Header always set Content-Security-Policy "\
        default-src 'self'; \
        script-src 'self' 'unsafe-inline' 'unsafe-eval' \
            https://www.googletagmanager.com \
            https://www.google-analytics.com \
            https://googleads.g.doubleclick.net \
            https://www.google.com \
            https://www.gstatic.com \
            https://maps.googleapis.com \
            https://maps.gstatic.com \
            https://widget.letsumai.com \
            https://js.stripe.com \
            https://www.google.com/recaptcha/ \
            https://www.gstatic.com/recaptcha/; \
        style-src 'self' 'unsafe-inline' \
            https://fonts.googleapis.com \
            https://widget.letsumai.com; \
        img-src 'self' data: blob: \
            https://www.google-analytics.com \
            https://www.googletagmanager.com \
            https://maps.googleapis.com \
            https://maps.gstatic.com \
            https://www.gstatic.com \
            https://*.google.com \
            https://*.googleapis.com \
            https://widget.letsumai.com \
            https://*.googleadservices.com; \
        font-src 'self' \
            https://fonts.gstatic.com; \
        connect-src 'self' \
            https://www.google-analytics.com \
            https://analytics.google.com \
            https://www.googletagmanager.com \
            https://maps.googleapis.com \
            https://widget.letsumai.com \
            https://api.letsumai.com \
            https://letsumai.com \
            https://*.stripe.com \
            https://www.google.com; \
        frame-src 'self' \
            https://www.google.com \
            https://maps.google.com \
            https://www.google.com/maps/ \
            https://widget.letsumai.com \
            https://js.stripe.com \
            https://hooks.stripe.com \
            https://www.googletagmanager.com; \
        worker-src 'self' blob:; \
        frame-ancestors 'self'; \
        base-uri 'self'; \
        form-action 'self';"

    # -------------------------------------------
    # Existing Security Headers (already had these)
    # -------------------------------------------
    
    # Prevent MIME type sniffing
    Header set X-Content-Type-Options "nosniff"
    
    # Clickjacking protection
    Header set X-Frame-Options "SAMEORIGIN"
    
    # XSS Protection (legacy, CSP is primary protection now)
    Header set X-XSS-Protection "1; mode=block"
    
    # Referrer Policy
    Header set Referrer-Policy "strict-origin-when-cross-origin"
    
    # Permissions Policy (formerly Feature-Policy) - UPDATED
    # Added payment permission for Stripe
    Header set Permissions-Policy "camera=(), microphone=(), geolocation=(), payment=(self https://js.stripe.com)"
</IfModule>

# ===========================================
# CACHING - CRITICAL FOR PERFORMANCE
# ===========================================

<IfModule mod_expires.c>
    ExpiresActive On
    
    # Default
    ExpiresDefault "access plus 1 month"
    
    # HTML - short cache (content changes)
    ExpiresByType text/html "access plus 0 seconds"
    
    # CSS - 1 year (Next.js uses hashed filenames)
    ExpiresByType text/css "access plus 1 year"
    
    # JavaScript - 1 year (Next.js uses hashed filenames)
    ExpiresByType application/javascript "access plus 1 year"
    ExpiresByType text/javascript "access plus 1 year"
    
    # Images - 1 year
    ExpiresByType image/webp "access plus 1 year"
    ExpiresByType image/png "access plus 1 year"
    ExpiresByType image/jpg "access plus 1 year"
    ExpiresByType image/jpeg "access plus 1 year"
    ExpiresByType image/gif "access plus 1 year"
    ExpiresByType image/svg+xml "access plus 1 year"
    ExpiresByType image/x-icon "access plus 1 year"
    
    # Fonts - 1 year
    ExpiresByType font/woff "access plus 1 year"
    ExpiresByType font/woff2 "access plus 1 year"
    ExpiresByType application/font-woff "access plus 1 year"
    ExpiresByType application/font-woff2 "access plus 1 year"
    
    # JSON/XML - short cache
    ExpiresByType application/json "access plus 0 seconds"
    ExpiresByType application/xml "access plus 0 seconds"
</IfModule>

# ===========================================
# CACHE-CONTROL HEADERS (More specific control)
# ===========================================

<IfModule mod_headers.c>
    # Next.js static chunks - IMMUTABLE (hashed filenames)
    <FilesMatch "^.*\.(js|css)$">
        Header set Cache-Control "public, max-age=31536000, immutable"
    </FilesMatch>
    
    # Images - long cache
    <FilesMatch "\.(webp|png|jpg|jpeg|gif|svg|ico)$">
        Header set Cache-Control "public, max-age=31536000, immutable"
    </FilesMatch>
    
    # Fonts - long cache
    <FilesMatch "\.(woff|woff2|ttf|eot)$">
        Header set Cache-Control "public, max-age=31536000, immutable"
    </FilesMatch>
    
    # HTML - no cache (always fetch fresh)
    <FilesMatch "\.html$">
        Header set Cache-Control "no-cache, no-store, must-revalidate"
        Header set Pragma "no-cache"
        Header set Expires "0"
    </FilesMatch>
</IfModule>

# ===========================================
# SPECIFIC NEXT.JS _next/static CACHING
# ===========================================

# This is CRITICAL - Next.js chunks were showing "None" for cache
<IfModule mod_headers.c>
    <If "%{REQUEST_URI} =~ m#^/_next/static/#">
        Header set Cache-Control "public, max-age=31536000, immutable"
    </If>
</IfModule>

# ===========================================
# COMPRESSION (GZIP/BROTLI)
# ===========================================

<IfModule mod_deflate.c>
    # Compress HTML, CSS, JavaScript, Text, XML and fonts
    AddOutputFilterByType DEFLATE text/html
    AddOutputFilterByType DEFLATE text/plain
    AddOutputFilterByType DEFLATE text/xml
    AddOutputFilterByType DEFLATE text/css
    AddOutputFilterByType DEFLATE application/javascript
    AddOutputFilterByType DEFLATE application/json
    AddOutputFilterByType DEFLATE application/xml
    AddOutputFilterByType DEFLATE application/xhtml+xml
    AddOutputFilterByType DEFLATE application/rss+xml
    AddOutputFilterByType DEFLATE application/atom+xml
    AddOutputFilterByType DEFLATE image/svg+xml
    AddOutputFilterByType DEFLATE font/ttf
    AddOutputFilterByType DEFLATE font/otf
    AddOutputFilterByType DEFLATE application/vnd.ms-fontobject
    
    # Remove browser bugs (only needed for really old browsers)
    BrowserMatch ^Mozilla/4 gzip-only-text/html
    BrowserMatch ^Mozilla/4\.0[678] no-gzip
    BrowserMatch \bMSIE !no-gzip !gzip-only-text/html
    
    # Don't compress already compressed files
    SetEnvIfNoCase Request_URI \.(?:gif|jpe?g|png|webp|zip|gz|bz2|rar)$ no-gzip
</IfModule>

# ===========================================
# ETAG CONFIGURATION
# ===========================================

# Remove ETags (cache headers are sufficient)
<IfModule mod_headers.c>
    Header unset ETag
</IfModule>
FileETag None

# ===========================================
# PREVENT DIRECTORY LISTING
# ===========================================

Options -Indexes

# ===========================================
# BLOCK SENSITIVE FILES
# ===========================================

# Block all hidden files (starting with .)
<FilesMatch "^\.">
    Order allow,deny
    Deny from all
</FilesMatch>

# Protect .env files specifically
<FilesMatch "^\.env">
    Order allow,deny
    Deny from all
</FilesMatch>

# Protect rate limits data
<FilesMatch "rate_limits\.json$">
    Order allow,deny
    Deny from all
</FilesMatch>

# Block other sensitive file types
<FilesMatch "\.(env|log|ini|bak|sql|sh|py)$">
    Order allow,deny
    Deny from all
</FilesMatch>
